{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the external Purr core consumer fixture",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "consumer-fixture:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
